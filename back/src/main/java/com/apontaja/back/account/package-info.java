/**
 * Domaine {@code account} — identité technique pure (email, mot de passe, vérification email).
 *
 * <p><b>Position dans le graphe de dépendances</b> (§2 du fichier de contexte) : racine, aucune
 * dépendance vers un autre domaine métier. {@code customer} dépend de {@code account}
 * (rattachement optionnel d'un {@code CustomerProfile} à un compte).
 */
package com.apontaja.back.account;
