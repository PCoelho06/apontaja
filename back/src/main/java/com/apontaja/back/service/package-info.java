/**
 * Domaine {@code service} — prestation proposée par le salon ({@code Service},
 * {@code ServiceResource}).
 *
 * <p>Nom de domaine métier tel qu'acté dans le fichier de contexte (§2) — à ne pas confondre
 * avec la couche {@code application} (services applicatifs) ni avec l'annotation Spring
 * {@code @Service}, qui n'ont pas de rapport de nommage direct avec ce package.
 *
 * <p><b>Position dans le graphe de dépendances</b> (§2) : dépend de {@code resource}.
 */
package com.apontaja.back.service;
