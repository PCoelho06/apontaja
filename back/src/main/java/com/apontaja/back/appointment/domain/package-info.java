/**
 * Couche {@code domain} du domaine {@code appointment} : entités JPA, value objects, règles
 * métier (conflit de créneau, gestion du temps : {@code Instant} pour les RDV, cf. §2), et ports
 * que {@code appointment.infrastructure} devra implémenter.
 */
package com.apontaja.back.appointment.domain;
