# ========================================
# Configuration PostgreSQL
# ========================================
POSTGRES_DB=apontaja_db
POSTGRES_USER=apontaja_user
# ⚠️ CHANGEZ avec un mot de passe fort (ex: pwgen -s 32 1)
POSTGRES_PASSWORD=CHANGEZ_MOI_AVEC_UN_MOT_DE_PASSE_FORT

# ========================================
# Configuration JWT (Spring Security)
# ========================================
# ⚠️ CHANGEZ avec une clé secrète longue et aléatoire (ex: openssl rand -base64 64)
JWT_SECRET=CHANGEZ_MOI_AVEC_UNE_CLE_SECRETE_LONGUE_ET_ALEATOIRE
# Durée de validité du token en millisecondes (86400000 = 24h)
JWT_EXPIRATION=86400000

# ========================================
# Configuration Email (SMTP)
# ========================================
# Exemples de configuration pour différents providers:
# Gmail: smtp.gmail.com (port 587, TLS)
# Outlook: smtp-mail.outlook.com (port 587, TLS)
# SendGrid: smtp.sendgrid.net (port 587, TLS)
# Mailgun: smtp.mailgun.org (port 587, TLS)

MAIL_HOST=smtp.example.com
MAIL_PORT=587
MAIL_USERNAME=votre-email@example.com
MAIL_PASSWORD=votre-mot-de-passe-email

# ========================================
# Configuration Frontend
# ========================================
# URL de l'API backend (utilisé lors du build du frontend)
VITE_API_URL=https://apontaja.com/api

# ========================================
# Configuration Domaine
# ========================================
DOMAIN=apontaja.com

# ========================================
# Configuration supplémentaire (optionnel)
# ========================================
# Environnement (dev, staging, production)
ENVIRONMENT=production

# Mode debug Spring Boot (true/false)
SPRING_DEBUG=false

# Niveau de log (ERROR, WARN, INFO, DEBUG, TRACE)
LOG_LEVEL=INFO